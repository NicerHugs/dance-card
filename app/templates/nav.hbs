{{#if user}}
  <div class="left-nav">
    {{#if user.organizer}}
        <a href="#/orgs/{{user.urlId}}" class="manage">manage your events</a>
        <a href="#/orgs/{{user.urlId}}/create-event" class="create">add an event</a>
    {{/if}}
        <a href="#" class="home-link">search for dances</a>
      </div>
  </div>
  <div class="right-nav">
    You are logged in as {{user.name}}. If that's not you, <a href="#/logout">logout</a>
    <a href="#/settings">settings</a>
  </div>
{{else}}
  <div class="left-nav">
    <a href="#" class="home-link">search for dances</a>
  </div>
  <div class="right-nav">
    <a href="#/login" class="login">login</a>
    <a href="#/register" class="signup">register</a>
  </div>
{{/if}}
