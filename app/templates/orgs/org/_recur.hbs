<h3>Your recurring events</h3>

{{#unless collection.models}}
  You have no recurring events.
  <a href="#/orgs/{{orgUrlId}}/create-event">Click here to add a new event</a>
{{/unless}}

{{#each collection.models}}
  <div class="recur-event"> 
    <a href="#" class="recurring">{{attributes.name}}</a>
    This event occurs every {{attributes.weeklyRptName}}
    <div class="event-details">
    </div>
  </div>
{{/each}}
